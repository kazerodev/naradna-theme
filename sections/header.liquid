<header class="site-header">
  <div class="container">
    <a href="/" class="header__logo">
      {%- if section.settings.logo != blank -%}
        <img src="{{ section.settings.logo | image_url: width: 160 }}" alt="{{ shop.name }}" width="120" height="40">
      {%- else -%}
        {{ shop.name }}
      {%- endif -%}
    </a>

    <nav>
      <ul class="header__nav">
        {%- for link in linklists[section.settings.menu].links -%}
          <li><a href="{{ link.url }}">{{ link.title }}</a></li>
        {%- endfor -%}
      </ul>
    </nav>

    <div class="header__actions">
      <button class="header__cart-btn" aria-label="Carrito">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>
        <span class="header__cart-count" style="display:none">0</span>
      </button>
      <button class="header__menu-btn" aria-label="Menú">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</header>

<div class="mobile-overlay"></div>
<nav class="mobile-menu">
  <button class="mobile-menu__close" aria-label="Cerrar">&#10005;</button>
  <ul class="mobile-menu__nav">
    {%- for link in linklists[section.settings.menu].links -%}
      <li><a href="{{ link.url }}">{{ link.title }}</a></li>
    {%- endfor -%}
    <li><a href="/cart">Carrito</a></li>
  </ul>
</nav>

{% schema %}
{
  "name": "Cabecera",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "link_list", "id": "menu", "label": "Menú", "default": "main-menu" }
  ]
}
{% endschema %}
